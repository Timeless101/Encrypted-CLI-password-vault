{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Password Vault",
            "type": "debugpy",
            "request": "launch",
            "module": "src.main",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}